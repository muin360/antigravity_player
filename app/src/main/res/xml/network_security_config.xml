<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration.

  Baseline: cleartext traffic is BLOCKED everywhere (Android default).

  The only exceptions are the local YouTube-extraction backend endpoints used
  during development (emulator loopback + device localhost). These exist so
  `backend/` can be tested on a LAN; they must never be relied upon in
  production. The production base URL is injected via BuildConfig and MUST be
  https:// (enforced in YtApiService).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
